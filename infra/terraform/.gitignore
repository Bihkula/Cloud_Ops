# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
plan.out
*.tfplan

# Never commit real variable values (may contain the DB password)
terraform.tfvars
*.auto.tfvars

# Keep the example
!terraform.tfvars.example
